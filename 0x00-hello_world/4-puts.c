#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 * puts: A function for displaying stdout
 */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";
 
puts(str);

return (0);
}
