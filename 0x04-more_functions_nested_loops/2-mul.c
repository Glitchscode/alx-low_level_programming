#include "main.h"
/**
 * mul - multiplies two integers
 * @a: it stores an integer
 * @b: It also stores an integer
 *
 * Return: returns the product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
