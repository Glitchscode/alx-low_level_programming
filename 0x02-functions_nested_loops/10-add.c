#include "main.h"
/**
 * add - adds two parameters together
 *
 * @n: holds an integer
 *
 * @a: holds an integer
 *
 * Return: it returns the addition of a and n in the main function
 */
int add(int n, int a)
{
	int result;

	result = n + a;
	return (result);
}
