#include "main.h"
/**
 * _isdigit - It returns 1 when the value is a digit and 0 when it is not
 * @c: parameter that stores the values
 *
 * Return: returns 1 or 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
	return (0);
}
